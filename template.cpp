#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pi = pair<int, int>;
using pl = pair<ll, ll>;
#define pb  push_back
#define mp make_pair
#define all(x) x.begin(), x.end()
#define forn(i,l,r) for(int (i)=(l); (i)<(r); (i)++)
#define rinc(i,l,r) for(int (i)=(l); (i)<=(r); (i)++)
#define rdec(i,r,l) for(int (i)=(r); (i)>=(l); (i)--)
#define deb(x) cerr<<#x<<" = "<<x<<endl;
const int MOD = 1e9 + 7; // 998244353
const int INF = 2e9+5; 
const int MAX = 2e5 + 7;

template<class T> 
bool ckmin(T &a, const T &b) { return b < a ? a = b, true : false; } // a := min(a, b)
template<class T> 
bool ckmax(T &a, const T &b) { return b > a ? a = b, true : false; } // a := min(a, b)


void solve(){

}
 
int main(){
	// ios_base::sync_with_stdio(0); cin.tie(NULL);
	int t = 1;
	//cin >> t;
	for(int tt=0;tt<t;tt++){
		//cout <<"Test Case #" << tt << ":\n";
		solve();
		cout << endl;
	}
}