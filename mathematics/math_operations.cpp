#include <bits/stdc++.h>
using namespace std;

using i64 = long long;

template<int mod>
T bin_exp(T x, i64 n) {
    T res = 1;
    while(b > 0) {
        if(b % 2) res *= x;
        x *= x, n /= 2;
    }
    return T;
}
 
int main(){
}